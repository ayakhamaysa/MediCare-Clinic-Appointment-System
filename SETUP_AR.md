# نظام حجز عيادة لطبيب واحد

## إنشاء نسخة مستقلة لدكتور جديد

تحتاجين إلى تعديل ملفين فقط:

1. `clinic.config.ts`: اسم العيادة والطبيب والتخصص والهاتف والبريد وكلمة المرور.
2. `database.config.json`: اسم الموقع، اسم قاعدة D1، ورقم قاعدة البيانات الذي تعرضه Cloudflare.

بعد إنشاء قاعدة D1 فارغة في حسابك، انسخي اسمها ورقمها إلى `database.config.json` ثم نفّذي:

```powershell
npm install
npx wrangler login
npm run deploy
```

أمر النشر يبني الموقع، يربطه بقاعدة البيانات المحددة، ويحذف أي ربط تجريبي تلقائيًا. عند فتح الموقع أول مرة ينشئ النظام الجداول داخل القاعدة تلقائيًا.

لكل دكتور انسخي المجلد كاملًا، عدّلي الملفين، وأنشئي قاعدة D1 جديدة. بهذه الطريقة لا تختلط بيانات أي دكتور مع الآخر.

هذا المشروع نظام كامل لحجز مواعيد عيادة، ويتكون من:

- صفحة عامة للمريض لاختيار التاريخ والوقت وإدخال الاسم ورقم الهاتف.
- صفحة دخول خاصة بالطبيب.
- لوحة تعرض الحجوزات والمواعيد القادمة فقط.
- صفحة يحدد منها الطبيب أيام الدوام وبداية ونهاية الدوام ومدة كل موعد.
- منع حجز الوقت نفسه مرتين.

## أين تُخزّن البيانات؟

النسخة المنشورة تستخدم **Cloudflare D1**، وهي قاعدة بيانات SQL سحابية مبنية على SQLite. لا يستخدم المشروع Firebase.

الجداول الأساسية هي:

- `appointments`: المواعيد المحجوزة.
- `patients`: اسم المريض ورقم الهاتف.
- `doctors`: بيانات الطبيب الواحد.
- `availability`: أيام وساعات الدوام ومدة الموعد.

ملفات تعريف قاعدة البيانات موجودة في `db/schema.ts`، وملفات إنشاء الجداول موجودة داخل مجلد `drizzle`.

## التشغيل محليًا

1. ثبّت Node.js إصدار 20 أو أحدث.
2. افتح مجلد المشروع في Terminal.
3. نفّذ `npm install`.
4. أضف متغيرات البيئة التالية إلى منصة الاستضافة التي ستستخدمها:

```env
DOCTOR_EMAIL=your-doctor-email@example.com
DOCTOR_PASSWORD=choose-a-strong-password
DOCTOR_SESSION_TOKEN=generate-a-long-random-secret
```

5. اربط قاعدة D1 بالاسم `DB`.
6. طبّق ملفات SQL الموجودة في مجلد `drizzle` على قاعدة البيانات.
7. للتطوير نفّذ `npm run dev`، وللتأكد من نسخة الإنتاج نفّذ `npm run build`.

## ملاحظة أمنية

لا تُكتب كلمة مرور الطبيب داخل الكود. تُحفظ كمتغير سري في إعدادات الاستضافة، ولذلك الملف المضغوط لا يعرض كلمة المرور الخاصة بالنسخة المنشورة.
